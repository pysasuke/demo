<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.py.demo.dao.UserMapper">

    <insert id="insert" parameterType="com.py.demo.model.User" keyProperty="id"
            useGeneratedKeys="true">
        insert into user (username,password,create_time,update_time)VALUES ( #{username,jdbcType=VARCHAR}, #{password,jdbcType=VARCHAR},now(),now())
    </insert>
    <select id="getById" resultType="com.py.demo.model.User">
        select id,username,password from user where id=#{id,jdbcType=BIGINT}
    </select>

</mapper>